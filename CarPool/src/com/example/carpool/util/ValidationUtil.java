package com.example.carpool.util;

/**
 * Handles all the validation for the user input.
 * @author ambika_b
 *
 */

public class ValidationUtil {

	/**
	 * checks if the user input is a valid email id or not.
	 * (in terms of the structure)
	 * @return
	 */
	public static Result isEmail(String email) {
		Result result = null;
		return result;
	}

	public static Result isPhoneNumber(String number) {
		Result result = null;
		return result;
	}

	public static Result isValidPassword(String password) {
		Result result = null;
		return result;
	}

	/**
	 * Checks for the length of the input
	 * @param input
	 * @return
	 */
	public static Result isValidInput(String input) {
		Result result = null;
		return result;
	}
}
